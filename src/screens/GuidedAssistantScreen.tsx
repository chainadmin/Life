import React from 'react'; import { Pressable, ScrollView, StyleSheet, Text } from 'react-native'; import { colors, card } from '../theme';
const prompts: Record<string, string[]> = { Today: ['Help me decide my priorities', 'Plan the rest of my day', 'Make a quick to-do list'], 'Write Something': ['Write an email', 'Rewrite a text', 'Make this sound professional', 'Write a complaint', 'Write a thank-you message'], 'Plan Something': ['Plan my day', 'Plan a trip', 'Make a checklist', 'Help me organize something'], 'Shopping Help': ['Compare two products', 'Help me decide what to buy', 'Find the best value', 'Make a shopping list'], 'Work Help': ['Write a work email', 'Create a checklist', 'Explain something simply', 'Help organize a project'] };
export function GuidedAssistantScreen({ navigation, route }: any) { const list = prompts[route.params.category] || []; return <ScrollView contentContainerStyle={s.page}><Text style={s.heading}>What would you like to do?</Text><Text style={s.sub}>Choose a starting point. You can explain the details next.</Text>{list.map(p => <Pressable key={p} style={s.prompt} onPress={() => navigation.navigate('Chat', { category: route.params.category, prompt: p })}><Text style={s.promptText}>{p}</Text><Text style={{ color: colors.green, fontSize: 22 }}>›</Text></Pressable>)}</ScrollView>; }
const s = StyleSheet.create({ page: { padding: 22, gap: 13 }, heading: { fontSize: 27, fontWeight: '800', color: colors.ink }, sub: { color: colors.muted, fontSize: 16, lineHeight: 23, marginBottom: 8 }, prompt: { ...card, flexDirection: 'row', justifyContent: 'space-between', alignItems: 'center', minHeight: 66 }, promptText: { color: colors.ink, fontSize: 17, fontWeight: '600' } });
