import React, { useEffect, useRef, useState } from 'react'; import { ActivityIndicator, KeyboardAvoidingView, Platform, Pressable, ScrollView, StyleSheet, Text, TextInput, View } from 'react-native'; import { colors } from '../theme'; import { api } from '../services/api'; import { Message } from '../types'; import { useAuth } from '../context/AuthContext';
export function ChatScreen({ route }: any) { const { profile } = useAuth(); const initial = route.params?.prompt; const [messages, setMessages] = useState<Message[]>(initial ? [{ id: 'intro', role: 'assistant', content: `I can help you ${initial.toLowerCase()}. Tell me what you need, and include any details you think matter.` }] : [{ id: 'intro', role: 'assistant', content: 'What would you like help with? You can ask in your own words.' }]); const [text, setText] = useState(''); const [loading, setLoading] = useState(false); const [error, setError] = useState(''); const [conversationId, setConversationId] = useState<string>(); const scroll = useRef<ScrollView>(null); useEffect(() => { scroll.current?.scrollToEnd({ animated: true }); }, [messages, loading]); async function send() { const content = text.trim(); if (!content || loading) return; const user: Message = { id: Date.now().toString(), role: 'user', content }; const next = [...messages, user]; setMessages(next); setText(''); setLoading(true); setError(''); try { const result = await api.chat({ message: content, conversationId, history: next.slice(-8).map(({ role, content }) => ({ role, content })), assistantPersonality: profile?.assistantPersonality, responseLength: profile?.responseLength }); setConversationId(result.conversationId); setMessages(m => [...m, { id: `${Date.now()}-a`, role: 'assistant', content: result.response }]); } catch (e) { setError(e instanceof Error ? e.message : 'I could not respond just now.'); } finally { setLoading(false); } } function reset() { setMessages([{ id: Date.now().toString(), role: 'assistant', content: 'A fresh start. What can I help with?' }]); setConversationId(undefined); setError(''); } return <KeyboardAvoidingView style={{ flex: 1, backgroundColor: colors.cream }} behavior={Platform.OS === 'ios' ? 'padding' : undefined} keyboardVerticalOffset={90}><View style={s.top}><Text style={s.context}>{route.params?.category || 'Ask Anything'}</Text><Pressable onPress={reset}><Text style={s.new}>＋ New conversation</Text></Pressable></View><ScrollView ref={scroll} contentContainerStyle={s.messages} keyboardShouldPersistTaps="handled">{messages.map(m => <View key={m.id} style={[s.bubble, m.role === 'user' ? s.user : s.assistant]}><Text style={[s.message, m.role === 'user' && { color: 'white' }]}>{m.content}</Text></View>)}{loading && <View style={[s.bubble, s.assistant, { flexDirection: 'row', gap: 10 }]}><ActivityIndicator color={colors.green} /><Text style={s.message}>Thinking…</Text></View>}{error ? <Text style={s.error}>{error} Tap send to try again.</Text> : null}</ScrollView><View style={s.composer}><TextInput value={text} onChangeText={setText} placeholder="Type what you need…" placeholderTextColor="#7B8581" multiline style={s.input} /><Pressable accessibilityLabel="Send message" onPress={send} style={s.send}><Text style={{ color: 'white', fontSize: 23 }}>↑</Text></Pressable></View></KeyboardAvoidingView>; }
const s = StyleSheet.create({ top: { paddingHorizontal: 18, paddingVertical: 10, flexDirection: 'row', justifyContent: 'space-between', borderBottomWidth: 1, borderColor: colors.border }, context: { color: colors.muted }, new: { color: colors.green, fontWeight: '700' }, messages: { padding: 16, gap: 12, flexGrow: 1 }, bubble: { padding: 14, borderRadius: 17, maxWidth: '86%' }, user: { backgroundColor: colors.green, alignSelf: 'flex-end', borderBottomRightRadius: 4 }, assistant: { backgroundColor: colors.white, alignSelf: 'flex-start', borderBottomLeftRadius: 4, borderWidth: 1, borderColor: colors.border }, message: { color: colors.ink, lineHeight: 22, fontSize: 16 }, error: { color: '#A83B3B', textAlign: 'center' }, composer: { flexDirection: 'row', alignItems: 'flex-end', gap: 9, padding: 12, borderTopWidth: 1, borderColor: colors.border, backgroundColor: colors.white }, input: { flex: 1, maxHeight: 110, minHeight: 48, padding: 13, borderRadius: 16, backgroundColor: colors.cream, color: colors.ink, fontSize: 16 }, send: { width: 48, height: 48, borderRadius: 15, backgroundColor: colors.green, alignItems: 'center', justifyContent: 'center' } });
