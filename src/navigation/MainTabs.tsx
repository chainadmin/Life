import React from 'react'; import { Text } from 'react-native'; import { createBottomTabNavigator } from '@react-navigation/bottom-tabs';
import { TabParamList } from '../types'; import { HomeScreen } from '../screens/HomeScreen'; import { MemoryScreen } from '../screens/MemoryScreen'; import { SettingsScreen } from '../screens/SettingsScreen'; import { colors } from '../theme';
import { TaskListScreen } from '../screens/TaskListScreen';
const Tab = createBottomTabNavigator<TabParamList>();
const icons: Record<string, string> = { Home: '⌂', Tasks: '✓', Memory: '♡', Settings: '⚙' };
export function MainTabs() { return <Tab.Navigator screenOptions={({ route }) => ({ headerShadowVisible: false, headerStyle: { backgroundColor: colors.cream }, headerTitleStyle: { color: colors.ink }, tabBarActiveTintColor: colors.green, tabBarInactiveTintColor: colors.muted, tabBarStyle: { height: 66, paddingBottom: 8, paddingTop: 5 }, tabBarIcon: ({ color }) => <Text style={{ color, fontSize: 22 }}>{icons[route.name]}</Text> })}><Tab.Screen name="Home" component={HomeScreen} options={{ headerShown: false }} /><Tab.Screen name="Tasks" component={TaskListScreen}/><Tab.Screen name="Memory" component={MemoryScreen} options={{ title: 'Things I Remember' }} /><Tab.Screen name="Settings" component={SettingsScreen} /></Tab.Navigator>; }
